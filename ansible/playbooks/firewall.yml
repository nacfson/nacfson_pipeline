---
# Firewall only: apply host nftables without remounting storage or installing k3s.
- name: Host firewall
  hosts: application:registry
  gather_facts: true
  become: true
  roles:
    - role: host_firewall
