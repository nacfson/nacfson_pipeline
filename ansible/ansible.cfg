[defaults]
inventory = inventories/home-lab/hosts.yaml
roles_path = roles
host_key_checking = True
interpreter_python = auto_silent
timeout = 30
retry_files_enabled = False
stdout_callback = default
result_format = yaml

[privilege_escalation]
become = True
become_method = sudo
