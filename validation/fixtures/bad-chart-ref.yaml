apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: bad-chart-ref
  namespace: fixture
spec:
  chart:
    spec:
      chart: ../outside/evil-chart
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
